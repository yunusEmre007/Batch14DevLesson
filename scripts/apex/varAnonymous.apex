//string firstVar = 'this is our first string variable';
//System.debug(firstVar);


string firstName = 'Emre';
string lastName = 'Onus';
String fullName = firstName + ' ' + lastName;
system.debug('Full name is ' + fullName);


string declareString; // declaring a variable
system.debug(declareString);

string defineString; // defining a variable
system.debug(defineString);